class Solution:
    def twoSum(self, nums: List[int], target: int):
        ans = []
        for i in range(len(nums)):
            for j in range(i):
                if target == (nums[i] + nums[j]):
                    ans.append(j)
                    ans.append(i)
        return ans
